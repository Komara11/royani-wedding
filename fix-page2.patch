--- src/app/page.tsx
+++ src/app/page.tsx
@@ -108,3 +108,3 @@
   const previewPkgs = packages.length > 0
-    ? packages.filter((p) => p.type?.toLowerCase() === "lengkap" || p.type?.toLowerCase() === "paket lengkap").slice(0, 3)
+    ? (packages.filter((p) => p.type?.toLowerCase().includes("lengkap")).length > 0 ? packages.filter((p) => p.type?.toLowerCase().includes("lengkap")).slice(0, 3) : packages.slice(0, 3))
     : [];
@@ -253,8 +253,4 @@
           <div className="pricing-preview-grid">
-            {(previewPkgs.length > 0 ? previewPkgs : [
-              { id: "1", name: "Silver", price: "Rp 15.000.000", featured: false, sections: [{ features: ["Pelaminan 6 Meter", "Rias Pengantin Lengkap", "MC & Sound System", "Dokumentasi Foto"] }] },
-              { id: "2", name: "Gold", price: "Rp 25.000.000", featured: true, sections: [{ features: ["Pelaminan 8 Meter", "Rias Pengantin Premium", "MC & Sound System", "Foto & Video", "Photobooth Corner"] }] },
-              { id: "3", name: "Platinum", price: "Rp 35.000.000", featured: false, sections: [{ features: ["Pelaminan 10 Meter Megah", "Rias Pengantin Exclusive", "MC & Full Entertainment", "Foto & Video Cinematic", "Full Dekorasi"] }] },
-            ] as any[]).map((pkg: any, i: number) => (
+            {previewPkgs.map((pkg: any, i: number) => (
               <AnimatedSection key={pkg.id} className={`pricing-card ${pkg.featured ? "featured" : ""}`} delay={i * 0.15}>
