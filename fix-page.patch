--- src/app/page.tsx
+++ src/app/page.tsx
@@ -82,2 +82,2 @@
-        if (heroData) setHeroContent((p) => ({ ...p, ...(heroData as any) }));
-        if (aboutData) setAboutContent((p) => ({ ...p, ...(aboutData as any) }));
+        if (heroData) setHeroContent((p) => ({ ...p, ...Object.fromEntries(Object.entries(heroData as any).filter(([_, v]) => v !== "" && v !== null)) }));
+        if (aboutData) setAboutContent((p) => ({ ...p, ...Object.fromEntries(Object.entries(aboutData as any).filter(([_, v]) => v !== "" && v !== null)) }));
@@ -103,3 +103,3 @@
   const previewPkgs = packages.length > 0
-    ? packages.filter((p) => p.type === "lengkap").slice(0, 3)
+    ? packages.filter((p) => p.type?.toLowerCase() === "lengkap" || p.type?.toLowerCase() === "paket lengkap").slice(0, 3)
     : [];
@@ -108,8 +108,8 @@
   const services = [
-    { icon: "💄", title: "Rias & Busana", description: "Tata rias pengantin profesional dengan sentuhan anggun, lengkap dengan busana pengantin pilihan terbaik." },
-    { icon: "🌸", title: "Dekorasi Pelaminan", description: "Dekorasi megah yang disesuaikan dengan tema pernikahan Anda, dari klasik hingga modern kontemporer." },
-    { icon: "📸", title: "Dokumentasi", description: "Abadikan setiap momen berharga dengan foto dan video berkualitas tinggi oleh tim fotografer berpengalaman." },
-    { icon: "🎵", title: "Entertainment", description: "MC profesional, sound system berkualitas, dan hiburan musik untuk memeriahkan acara pernikahan Anda." },
+    { icon: "I", title: "Rias & Busana", description: "Tata rias pengantin profesional dengan sentuhan anggun, lengkap dengan busana pengantin pilihan terbaik." },
+    { icon: "II", title: "Dekorasi Pelaminan", description: "Dekorasi megah yang disesuaikan dengan tema pernikahan Anda, dari klasik hingga modern kontemporer." },
+    { icon: "III", title: "Dokumentasi", description: "Abadikan setiap momen berharga dengan foto dan video berkualitas tinggi oleh tim fotografer berpengalaman." },
+    { icon: "IV", title: "Entertainment", description: "MC profesional, sound system berkualitas, dan hiburan musik untuk memeriahkan acara pernikahan Anda." },
   ];
